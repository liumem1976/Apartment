《任务假设与范围》
- 假设：本分支为自动格式化与小幅修复分支，基于 `task-b-domain-models` 工作流生成。
- 范围：应用 ruff/isort/black 自动修复、清理被跟踪的 `__pycache__` 文件、更新 `.gitignore`；不包含功能性逻辑更改。

《设计与决策》
- 原则：仅执行自动可修复项与非侵入性清理，保持最小变更原则。
- 决策：将格式化与样式修复独立为单一 PR，便于快速审查并保持功能变更与样式分离。

《代码与文件结构》
- 修改文件（摘要）：
  - `.gitignore`：添加常见 Python/IDE/构建忽略项。
  - `scripts/check_duplicate_meters.py`：添加/格式化用于重复检查的脚本（无逻辑变更，仅整理）。
  - `scripts/list_tables.py`：新增，辅助验证数据库迁移结果。
  - 若干自动格式化修改（由 ruff/isort/black 作用在仓库文件上）。

《测试与验证》
- 本地已运行：
  - `ruff check --fix`、`isort`、`black` 已执行并自动修复可修复项。
  - `pytest` 本地通过：`12 passed, 2 warnings`。
- 建议：CI 应继续执行现有的 lint 与测试步骤以二次确认。

《运行与维护》
- 无需额外运行时更改。负责人在合并后可继续按既有 CI 流程验证。若后续需回滚，此 PR 为样式/清理类变更，风险较低。

《安全与一致性清单》
- ✅ 迁移/模型：无新增模型迁移在此 PR 中。
- ✅ 代码风格：已通过本地自动修复工具（ruff/black/isort）。
- ✅ 测试：本地测试通过（12 passed）。
- ✅ 依赖：未更改依赖文件。
- ⚠️ 注意：已移除被跟踪的 `__pycache__` 文件并更新 `.gitignore`，请确认 CI 环境不会依赖被忽略的临时文件。

《后续迭代建议》
- 若无异议，建议在 CI 通过后合并本 PR（squash 合并即可）。
- 建议在合并 `task-b-domain-models` 前，对 `0004_add_meter_unique.py` 在生产副本上做重复性检查（已有独立脚本）。

---
自动生成：由项目 AI 助手基于自动格式化与校验步骤创建。若需更改 PR 目标分支或说明，请在评论中指示。